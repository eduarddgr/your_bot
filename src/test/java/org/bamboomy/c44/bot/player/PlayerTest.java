package org.bamboomy.c44.bot.player;

import org.junit.Test;

public class PlayerTest {

	@Test
	public void testDeath() {

		/*
		 * 
		 * Board board = mock(Board.class); Calculator calculator =
		 * mock(Player.Calculator.class);
		 * 
		 * Alliance alliance = new Alliance(Color.GREEN, Color.RED);
		 * 
		 * when(board.getAlliance()).thenReturn(alliance);
		 * 
		 * Player player = new Player(0, board);
		 * 
		 * MoveCalculator moveCalculator = player.new MoveCalculator(board, Move move,
		 * Color.GREEN, alliance, 0, calculator, 0);
		 * 
		 * moveCalculator.evaluate(0, 0, 0, 0, false, false);
		 * 
		 */

		System.out.println("tested...");
	}
}
